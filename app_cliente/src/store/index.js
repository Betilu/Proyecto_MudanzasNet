// Configuración de store (Redux, Zustand, etc.)
// Ejemplo con Zustand:
// import { create } from 'zustand';
//
// export const useStore = create((set) => ({
//   user: null,
//   setUser: (user) => set({ user }),
// }));
